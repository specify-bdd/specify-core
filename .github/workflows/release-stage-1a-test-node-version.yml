name: Release Stage 1A - Test Node Version
on:
  workflow_call:
    inputs:
      node_version:
        description: "The Node.js version to test"
        required: true
        type: string
      store_artifacts:
        description: "Store the node_modules and dist directories for use later."
        type: boolean
        default: false
permissions:
  contents: read
jobs:
  test-node:
    name: Test Node.js version ${{ inputs.node_version }}
    runs-on: ubuntu-${{ vars.UBUNTU_VERSION }}
    env:
      GIT_BRANCH: develop
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
        with:
          ref: ${{ env.GIT_BRANCH }}
      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          run_install: true
      - name: Setup Node.js ${{ inputs.node_version }}
        uses: actions/setup-node@v4
        with:
          cache: pnpm
          node-version: ${{ inputs.node_version }}
          registry-url: "https://registry.npmjs.org"
      - name: Generate types
        run: pnpm generate-types
      - name: Build
        run: pnpm build
      - name: Test
        run: pnpm test
      - name: Upload build artifacts
        if: ${{ inputs.store_artifacts == true }}
        uses: actions/upload-artifact@v4
        with:
          name: artifact_build_${{ github.run_id }}
          path: ./modules/@specify-bdd/*/dist
          include-hidden-files: true

